# Minimum runtime dependencies for PyGuard
# Core capture & processing
scapy>=2.5.0
pcapy-ng>=1.0.9
dpkt>=1.9.8
netifaces>=0.11.0

# System and utilities
psutil>=5.9.0
python-dateutil>=2.8.2
PyYAML>=6.0
colorlog>=6.7.0
tqdm>=4.64.1

# Data handling
pandas>=1.5.0
numpy>=1.24.0
pyarrow>=10.0.0

# Storage / DB
psycopg2-binary>=2.9.5
sqlalchemy>=2.0.0
cicflowmeter>=0.2.0

# GUI (project contains code for both PyQt5 and PyQt6 UIs)
PyQt5>=5.15.7; platform_system == "Windows" or platform_system == "Linux" or platform_system == "Darwin"
PyQt6>=6.5.0; platform_system == "Windows" or platform_system == "Linux" or platform_system == "Darwin"

# Machine learning (optional - only needed for ML workflows)
torch>=2.2.0
scikit-learn>=1.3.0
torch-geometric>=2.5.0
joblib>=1.3.0

# Notes:
# - Keep the virtual environment isolated and install these using pip inside venv.
# - If you only use the desktop app, you may omit the ML packages to save space.
scapy>=2.5.0
pcapy-ng>=1.0.9
psutil>=5.9.0
psycopg2-binary>=2.9.5
PyYAML>=6.0
pandas>=1.5.0
pyarrow>=10.0.0
PyQt5>=5.15.7
dpkt>=1.9.8
netifaces>=0.11.0
python-dateutil>=2.8.2
sqlalchemy>=2.0.0
tqdm>=4.64.1
colorlog>=6.7.0
cicflowmeter>=0.2.0

# ML dependencies
torch>=2.2.0
scikit-learn>=1.3.0
torch-geometric>=2.5.0
joblib>=1.3.0